# AI-1C-Code-Generation-Benchmark

Экспериментальный фреймворк для оценки качества генерации кода на платформе **1С:Предприятие 8.3** различными LLM-моделями. Разработан в рамках научного исследования «Экспериментальная оценка эффективности искусственного интеллекта в генерации кода для доменно-специфичных платформ (на примере 1С:Предприятие 8)».

---

## 🎯 О проекте

Фреймворк решает задачу **объективной оценки способности** современных больших языковых моделей (LLM) генерировать корректный и качественный код для специфичной платформы 1С:Предприятие.

### Ключевые возможности

- ✅ **Автоматизированная оценка** — запуск задач, генерация кода, анализ детерминизма
- ✅ **SMOP-метрика** — экспертная оценка по 4 критериям (Syntax, Meaning, Optimization, Platform)
- ✅ **Две категории задач**:
  - **Категория A**: Алгоритмические задачи (без контекста платформы)
  - **Категория B**: Платформенные задачи (требуют знания метаданных через MCP)
- ✅ **Поддержка 3 моделей**: Claude Opus 4.5, GPT-5.2 Codex, Gemini 3 Flash
- ✅ **Детальная аналитика** — статистика, графики, HTML/LaTeX отчёты

---

## 🚀 Быстрый старт

### 1. Установка

```bash
git clone https://github.com/4dand/AI-1C-Code-Generation-Benchmark.git
cd AI-1C-Code-Generation-Benchmark
pip install -r requirements.txt
```

### 2. Настройка

Создайте файл `.env` в корне проекта:

```env
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ-openrouter
```

> 🔑 Получить ключ: [openrouter.ai/keys](https://openrouter.ai/keys)

### 3. Запуск эксперимента

```bash
# Запустить все задачи категории A для модели Gemini
python main.py run -c A -m gemini

# Запустить конкретную задачу для всех моделей
python main.py run -c A -t A1 --all-models

# Запустить задачи категории B (требуется MCP-сервер)
python main.py run -c B -m claude --no-mock
```

### 4. Результаты

После запуска эксперимента результаты сохраняются в:

```
raw_results/experiment_A_YYYYMMDD_HHMMSS.json     # Сырые результаты
code_outputs/experiment_A_YYYYMMDD_HHMMSS/        # Сгенерированный код, доступный для экспорта
logs/experiment_A_YYYYMMDD_HHMMSS.log             # Логи выполнения
```

Для построения отчётов используйте:

```bash
python main.py report experiment_A_YYYYMMDD_HHMMSS
```

---

## 📊 Поддерживаемые модели

| Модель | ID OpenRouter | Детерминизм | Контекст |
|--------|---------------|-------------|----------|
| **Claude Opus 4.5** | `anthropic/claude-opus-4.5` | temperature=0 | 200K |
| **GPT-5.2 Codex** | `openai/gpt-5.2-codex` | seed | 400K |
| **Gemini 3 Flash** | `google/gemini-3-flash-preview` | seed | 1M |

---

## � Структура проекта

```
configs/                    # Конфигурационные файлы
├── experiment.yaml         # Метаданные эксперимента
├── models.yaml             # Настройки моделей
├── tasks_category_A.yaml   # Задачи категории A
├── tasks_category_B.yaml   # Задачи категории B
└── smop_criteria.yaml      # Критерии оценки SMOP

src/
├── clients/                # Клиенты API (OpenRouter, MCP)
├── core/                   # Основная логика (BenchmarkRunner)
├── evaluator/              # Модуль оценки (SMOP, статистика, отчёты)
├── schemas/                # Pydantic-схемы данных
└── utils/                  # Утилиты (хеширование, экспорт)

code_outputs/               # Сгенерированный код (.bsl файлы)
raw_results/                # JSON с результатами экспериментов
evaluations/                # JSON с экспертными оценками SMOP
reports/                    # HTML/LaTeX отчёты, графики
logs/                       # Логи выполнения
```

---

## 🔧 Основные команды CLI

```bash
# Информация
python main.py info --balance          # Баланс OpenRouter
python main.py info --models           # Список моделей
python main.py info --tasks A          # Задачи категории A

# Запуск
python main.py run -c A -m gemini      # Эксперимент категории A
python main.py run -c B --all-models   # Все модели категории B (с mock mcp)

# Отчёты
python main.py report <experiment_id>  # Построить отчёт
```

---

## 📐 Методология оценки (SMOP)

Качество сгенерированного кода оценивается экспертами по **4 критериям**:

| Критерий | Описание | Вес |
|----------|----------|-----|
| **S** (Syntax) | Синтаксическая корректность кода | 1.0 |
| **M** (Meaning) | Семантическая корректность (соответствие логики заданию) | 1.0 |
| **O** (Optimization) | Оптимальность (стандарты разработки 1С) | 1.0 |
| **P** (Platform) | Платформенная интеграция (корректность работы с метаданными) | 1.0 |

Каждый критерий оценивается по шкале: **0, 2, 4, 6, 8, 10**.

Интегральный показатель качества:
```
Q = (S + M + O + P) / 4
```

> 📚 Детальное описание критериев: `configs/smop_criteria.yaml`

---

## 🔬 Категория B и MCP-сервер

Задачи **категории B** требуют доступа к метаданным конфигурации 1С (УТ 11.5). Для этого используется **MCP-сервер** (Model Context Protocol), который предоставляет LLM актуальную информацию о структуре объектов конфигурации.

### Запуск MCP-сервера (Docker)

```bash
docker pull vladimir-kharin/1c_mcp
docker run -d -p 8000:8000 vladimir-kharin/1c_mcp
```

> Фреймворк автоматически использует mock-данные, если MCP-сервер недоступен.

---

## 📖 Детальная документация

Для углубленного изучения архитектуры, методологии и технических деталей см. папку **`docs/`**:

- **`docs/architecture.md`** — Архитектура системы, модули, взаимодействие компонентов
- **`docs/evaluator.md`** — Модуль оценки: SMOP, формулы статистики, алгоритмы
- **`docs/methodology.md`** — Научная методология: дизайн эксперимента, метрики
- **`docs/api.md`** — API-документация для разработчиков
- **`docs/contributing.md`** — Руководство для контрибьюторов

> 🚧 Документация находится в разработке и будет дополняться.

---

## 📊 Пример отчёта

После завершения эксперимента генерируются:

- **HTML-отчёт** с интерактивными графиками
- **LaTeX-таблицы** для научных публикаций
- **JSON с метриками** для дальнейшего анализа

Пример структуры отчёта:

```
reports/
├── experiment_A_20260205_232949_report.html    # Веб-отчёт
├── experiment_A_20260205_232949_report.json    # Метрики
├── experiment_A_20260205_232949_tables.tex     # LaTeX-таблицы
└── charts/
    └── experiment_A_20260205_232949/
        ├── smop_comparison.png                 # Сравнение по SMOP
        ├── determinism_analysis.png            # Анализ детерминизма
        └── cost_analysis.png                   # Стоимость генерации
```

---

## 🤝 Контрибьюция

Проект открыт для улучшений:

1. Добавление новых задач в `configs/tasks_category_*.yaml`
2. Интеграция новых моделей в `configs/models.yaml`
3. Расширение критериев оценки в `configs/smop_criteria.yaml`
4. Улучшение модуля аналитики (`src/evaluator/`)

> Подробнее: `docs/contributing.md` (в разработке)

---

## 📄 Лицензия

MIT License

---

## 👤 Авторы

**Андреев Данила**, **Гальцкова Юлия Михайловна**
Научный проект: *Экспериментальная оценка эффективности ИИ в генерации кода для доменно-специфичных платформ (на примере 1С:Предприятие 8)*

---

## 📮 Контакты

- GitHub Issues: [github.com/4dand/AI-1C-Code-Generation-Benchmark/issues](https://github.com/4dand/AI-1C-Code-Generation-Benchmark/issues)

---

**⚡ Готовы к экспериментам?** Запустите свой первый бенчмарк:

```bash
python main.py run -c A -m gemini -t A1
```